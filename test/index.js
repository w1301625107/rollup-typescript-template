// var assert = require('assert');
import * as assert from 'assert'

describe('openProtocol', function () {
  before(function () {
    // do something 
  })

  after(function () {
     // do something 
  })

  describe('#Test Function', function () {
    it('should success', function (done) {
      assert.equal(1,1)
      done()
    })
  })
})
